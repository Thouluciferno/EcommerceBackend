package com.edu.ecommerce.mapper;


import com.edu.ecommerce.entity.InvalidatedToken;
import org.mapstruct.Mapper;

import java.util.List;

@Mapper(componentModel = "spring")
public interface InvalidatedTokenMapper {

//    chưa cần động dùng danh sach invalidatedToken
//    toInvalidatedToken(InvalidatedToken invalidatedToken);
//
//    toInvalidatedTokenList(List<InvalidatedToken> invalidatedTokenList);

}
